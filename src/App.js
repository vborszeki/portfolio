import React, { Component } from 'react';
import StartPage from './StartPage';

class App extends Component {
  render() {
    return (
      <div>
        <StartPage />
      </div>
    );
  }
}

export default App;
